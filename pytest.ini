[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    network: tests that require internet access (PyPI / YouTube) — auto-skipped when offline
    slow: tests that take more than a few seconds (subset of network)
